<resources>

    <!-- Base application theme. -->
    <style name="Base.Theme.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/dark_primary_color</item>
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorAccent">@color/accent_color</item>
    </style>

    <style name="AppTheme" parent="Base.Theme.AppTheme">

    </style>

    <style name="SignInTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:colorBackground">@color/primary_color</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="colorButtonNormal">@color/dark_primary_color</item>
    </style>

    <style name="SignInProgreeDialogTheme" parent="Theme.AppCompat.Light.Dialog">

    </style>

</resources>
